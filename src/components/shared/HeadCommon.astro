---
import { ClientRouter } from 'astro:transitions';
import { Font } from 'astro:assets';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  ogTitle?: string;
  ogDescription?: string;
  canonicalUrl?: string;
  themeColor?: string;
}

const {
  title,
  description = 'Walker 的个人航海日志，记录 AI 探索、独立开发与生活点滴。',
  ogImage = '/images/hero-bg.png',
  ogTitle,
  ogDescription,
  canonicalUrl,
  themeColor = '#35bfab',
} = Astro.props;

const pageTitle = ogTitle ?? title;
const pageDescription = ogDescription ?? description;
const canonical = canonicalUrl ?? Astro.url.href;
const absoluteOgImage = new URL(ogImage, Astro.site ?? Astro.url).href;
---

<meta charset="utf-8" />
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
<meta name="generator" content={Astro.generator} />
<meta name="description" content={description} />
<meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1" />
<meta name="theme-color" content={themeColor} />
<meta name="author" content="Walker (秋知)" />
<link rel="canonical" href={canonical} />
<link rel="alternate" type="application/rss+xml" title="Walker RSS" href={new URL('/rss.xml', Astro.site ?? Astro.url)} />

<Font cssVariable="--font-body" preload />
<Font cssVariable="--font-heading" preload />
<Font cssVariable="--font-mono" />
<Font cssVariable="--font-cjk" />

<meta property="og:title" content={pageTitle} />
<meta property="og:description" content={pageDescription} />
<meta property="og:image" content={absoluteOgImage} />
<meta property="og:url" content={canonical} />
<meta property="og:type" content="website" />
<meta property="og:site_name" content="Walker 知识空间" />
<meta property="og:locale" content="zh_CN" />
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={pageTitle} />
<meta name="twitter:description" content={pageDescription} />
<meta name="twitter:image" content={absoluteOgImage} />

{/* 活体主题防闪屏：同步内联脚本，paint 前套上 localStorage 缓存的配色 + 昼夜标记 */}
<script is:inline set:html={`(function(){try{var r=localStorage.getItem('walker-ambient-vars');if(r){var v=JSON.parse(r),d=document.documentElement;for(var k in v)d.style.setProperty(k,v[k]);}if(localStorage.getItem('walker-ambient-night')==='1')d.setAttribute('data-ambient','night');}catch(e){}})();`} />

<ClientRouter />
<title>{title}</title>
