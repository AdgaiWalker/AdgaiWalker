---
/**
 * /about — 关于（重设计版）
 * 单页长卷叙事：Hero → 简介 → 时间轴 → Bento Grid → Pipeline → Footer
 * 使用 FullscreenLayout
 */
import FullscreenLayout from '../../layouts/FullscreenLayout.astro';
import LikeCounter from '../../components/LikeCounter.astro';
import SectionHeader from '../../components/about/SectionHeader.astro';
import { Icon } from 'astro-icon/components';
import { Image } from 'astro:assets';
import avatar from '/public/images/秋知（我的头像）.jpg';
import stats from '@/data/site-stats.json';
import { articles, pillars, aiTools, totalCost, costByCategory } from '@/data/site-data';

export const prerender = true;

const { personalTimeline, siteTimeline } = stats;
---

<FullscreenLayout
  title="关于 — Walker"
  description="秋知 (Walker) — 用点子连接人与 AI，将想法点石成金。"
>

  {/* ===== 1. Hero 区域 ===== */}
  <section class="hero-section" id="hero-section">
    <div class="hero-bg">
      <video
        class="hero-bg-video"
        autoplay
        muted
        loop
        playsinline
        preload="auto"
        poster="/video/A-storyboard-2.mp4"
      >
        <source src="/video/A-storyboard-2.mp4" type="video/mp4" />
      </video>
      <div class="hero-overlay"></div>
    </div>

    <div class="hero-content">
      <div class="hero-avatar-wrapper">
        <div class="hero-avatar-ring">
          <Image src={avatar} alt="秋知" width={100} height={100} class="hero-avatar-img" />
        </div>
      </div>
      <h1 class="hero-title">Walker</h1>
      <p class="hero-typing" id="hero-typing" aria-label="用点子连接人与 AI"></p>
      <p class="hero-sub" id="hero-sub">赋能，不依赖 — 让每个人都能用 AI 解决真实问题</p>
      <p class="hero-era">一个人 + AI，240 元，从零搭了这整个站。你也可以。</p>
      <div class="hero-actions" id="hero-actions">
        <a href="#why-section" class="hero-btn hero-btn--primary">
          <Icon name="lucide:compass" width={15} height={15} />
          开始探索
        </a>
        <a href="/tools#info-source" class="hero-btn hero-btn--ghost">
          <Icon name="lucide:users" width={15} height={15} />
          点子共促
        </a>
      </div>
    </div>

    <div class="hero-scroll-hint" id="scroll-hint">
      <Icon name="lucide:chevron-down" width={24} height={24} />
    </div>

    <div class="hero-gradient-bar"></div>
  </section>

  {/* ===== 2. 关于我 ===== */}
  <section class="about-bio-section" id="bio-section">
    <div class="about-container">
      <SectionHeader title="秋知 Walker" />

      <div class="bio-card panel-glass">
        <div class="bio-narrative">
          <p>Walker 不是一个人名，而是一种状态：带着自己的目标，准备出发、正在出发、歇会再出发。</p>
          <p>空乘专业给了我时间，马哲给了我方法，AI 给了我路。</p>
          <p>我不是在学 AI，而是在用 AI 解决真实问题。</p>
        </div>

        <div class="bio-quote">
          <div class="bio-quote-mark" aria-hidden="true">"</div>
          <p class="bio-quote-text">哲学家们只是用不同的方式解释世界，问题在于改变世界。</p>
          <p class="bio-quote-author">—— 马克思</p>
        </div>

        <div class="bio-gratitude panel-glass">
          <div class="bio-gratitude-stamp">航行信戳</div>
          <h4 class="bio-gratitude-title">
            <Icon name="lucide:heart" width={16} height={16} />
            致我的引路人与恩师
          </h4>
          <p class="bio-gratitude-text">
            "一路走来，秋芝引我入门，海哥（海拉鲁编程客）带我成长，刘、于二老在迷茫时点醒方向。感谢虽不理解但给予经济支持的亲人。水路万里，停靠因你们而有据可循。"
          </p>
        </div>

        <div class="bio-social">
          <a href="https://github.com/AdgaiWalker" target="_blank" rel="noopener noreferrer" class="social-pill">
            <Icon name="lucide:github" width={16} height={16} />
            <span>GitHub</span>
          </a>
          <a href="https://space.bilibili.com/1029612512" target="_blank" rel="noopener noreferrer" class="social-pill social-pill--bili">
            <Icon name="lucide:circle-play" width={16} height={16} />
            <span>Bilibili</span>
          </a>
          <a href="https://www.xiaohongshu.com/user/profile/689dd905000000001802921e" target="_blank" rel="noopener noreferrer" class="social-pill social-pill--xhs">
            <Icon name="lucide:book-open" width={16} height={16} />
            <span>小红书</span>
          </a>
          <button type="button" class="social-pill social-pill--email" id="copy-email">
            <Icon name="lucide:mail" width={16} height={16} />
            <span>Email</span>
          </button>
        </div>
      </div>
    </div>
  </section>

  {/* ===== 3. 航行日志 · 时间轴 ===== */}
  <section class="journey-section" id="journey-section">
    <div class="about-container">
      <SectionHeader title="航行日志 · 正在铺的路" />

      {/* 航行者宣言 */}
      <div class="manifesto panel-glass">
        <div class="manifesto-inner">
          <div class="manifesto-icon">
            <Icon name="lucide:compass" width={20} height={20} />
          </div>
          <div>
            <p class="manifesto-text">" 最好的生活状态是，带着自己的目标，准备出发、正在出发、歇会再出发。 "</p>
            <p class="manifesto-source">— VOYAGER'S SPIRITUAL MANIFESTO / 航行者宣言</p>
          </div>
        </div>
        <div class="manifesto-states">
          <div class="manifesto-state">
            <span class="state-dot state-dot--amber"></span>
            准备出发
          </div>
          <div class="manifesto-state manifesto-state--active">
            <span class="state-dot state-dot--brand"></span>
            正在出发
          </div>
          <div class="manifesto-state">
            <span class="state-dot state-dot--emerald"></span>
            歇会再出发
          </div>
        </div>
      </div>

      {/* 时间轴 */}
      <div class="timeline-container">
        <div class="timeline-track" aria-hidden="true"></div>
        <div class="timeline-progress" id="timeline-progress" aria-hidden="true"></div>

        {personalTimeline.map((item, i) => {
          const isEven = i % 2 === 0;
          const hasChildren = item.children && item.children.length > 0;
          return (
            <div class:list={['timeline-item', { 'timeline-item--right': !isEven }]}>
              <div class="timeline-dot">
                <span class="timeline-dot-core"></span>
                <span class="timeline-dot-ripple"></span>
              </div>

              <div class="timeline-date-col">
                <span class="timeline-date">{item.date}</span>
              </div>

              <div class="timeline-card-col">
                <div class="timeline-card panel-glass">
                  <h4 class="timeline-card-title">
                    <Icon name={item.icon} width={14} height={14} />
                    {item.title}
                  </h4>
                  {hasChildren ? (
                    <div class="timeline-children">
                      {item.children.map((child: { label: string; text: string; href?: string }) => (
                        <div class="timeline-child">
                          {child.href ? (
                            <a href={child.href} target="_blank" rel="noopener noreferrer" class="timeline-child-label">{child.label}</a>
                          ) : (
                            <span class="timeline-child-label">{child.label}</span>
                          )}
                          <span class="timeline-child-text">{child.text}</span>
                        </div>
                      ))}
                    </div>
                  ) : (
                    <p class="timeline-card-desc">{item.desc}</p>
                  )}
                </div>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  {/* ===== 4. 航行誓言 · 为什么建 ===== */}
  <section class="why-section" id="why-section">
    <div class="about-container">
      <SectionHeader title="航行誓言 · 为什么建" />

      <div class="why-story panel-glass">
        <p>从去年（25年）九月开始，我一直在飞书写学习记录，建了好几个知识库。飞书很好用，但 15G 不够用，而且它没法按我的想法长成我想要的样子。</p>
        <p>后来看到 lvy01 的博客，他的内容就是 his 个人介绍，既是知识库又是名片。还有海哥的博客，像一个小型飞书，但比飞书灵活，足够承载他的内容。</p>
        <p>我想，我已经会用 AI 编程了，为什么不自己搭一个？</p>
        <p>对内，沉淀之后能反哺自己——省时间、提效率、保证做事的质量。对外，就是我的个人介绍。也满足我的好奇心：用好奇心去创造，创造出新的东西让自己继续好奇。</p>
      </div>

      <p class="why-articles-hint">这三篇日志记录了完整的远征来龙去脉</p>
      <div class="why-articles-grid">
        {articles.map((a) => (
          <a href={a.href} class="why-article panel-glass">
            <Icon name="lucide:file-text" width={14} height={14} class="text-brand" />
            <div>
              <h4 class="why-article-title">{a.title}</h4>
              <p class="why-article-summary">{a.summary}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  {/* ===== 5. 赋能 CTA ===== */}
  <section class="cta-section" id="cta-section">
    <div class="about-container">
      <div class="cta-card">
        <div class="cta-card-inner">
          <div class="cta-text-col">
            <h2 class="cta-headline">点子共促 · 一起学 AI</h2>
            <p class="cta-philosophy">赋能，不依赖。</p>
            <p class="cta-desc">带着自己的目标来，用 AI 解决你自己的真实问题。这里不教你依赖任何工具，而是帮你学会用工具武装自己。</p>
            <div class="cta-highlights">
              <div class="cta-highlight">
                <Icon name="lucide:lightbulb" width={16} height={16} />
                <span>点子共创 — 抛出各自的点子，看可行性，有人想一起做就一起做</span>
              </div>
              <div class="cta-highlight">
                <Icon name="lucide:rocket" width={16} height={16} />
                <span>点子共促 — 带着目的一起学 AI，遇到问题互相交流</span>
              </div>
            </div>
            <div class="cta-actions">
              <a href="/tools#info-source" class="cta-btn cta-btn--primary">
                <Icon name="lucide:arrow-right" width={15} height={15} />
                了解更多
              </a>
              <div class="cta-social-row">
                <a href="https://github.com/AdgaiWalker" target="_blank" rel="noopener noreferrer" class="cta-social-icon" title="GitHub">
                  <Icon name="lucide:github" width={16} height={16} />
                </a>
                <a href="https://space.bilibili.com/1029612512" target="_blank" rel="noopener noreferrer" class="cta-social-icon" title="Bilibili">
                  <Icon name="lucide:circle-play" width={16} height={16} />
                </a>
                <a href="https://www.xiaohongshu.com/user/profile/689dd905000000001802921e" target="_blank" rel="noopener noreferrer" class="cta-social-icon" title="小红书">
                  <Icon name="lucide:book-open" width={16} height={16} />
                </a>
              </div>
            </div>
          </div>
          <div class="cta-qr-col">
            <div class="cta-qr-wrapper">
              <img src="/images/qr/dianzi-gongcu.jpg" alt="点子共促群二维码" class="cta-qr-img" loading="lazy" />
              <p class="cta-qr-label">扫码加入点子共促</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* ===== 6. 装配舱单 · Bento Grid ===== */}
  <section class="bento-section" id="bento-section">
    <div class="about-container">
      <SectionHeader title="装配舱单 · 这个站是什么" />

      {/* 建站动机 */}
      <div class="bento-motivation panel-glass">
        <p>对内是整理思考、沉淀资源、酝酿点子的<strong>前进系统</strong>。</p>
        <p>对外是给想用 AI 解决真实问题的人的<strong>参考</strong>。</p>
      </div>

      {/* 四柱 Bento Grid */}
      <div class="bento-grid">
        {pillars.map((p) => (
          <a href={p.route} class="bento-cell panel-glass">
            <div class="bento-cell-icon">
              <Icon name={p.icon} width={20} height={20} />
            </div>
            <div class="bento-cell-body">
              <div class="bento-cell-header">
                <span class="bento-cell-label">{p.label}</span>
                <span class="bento-cell-hint">{p.hint}</span>
              </div>
              <p class="bento-cell-desc">{p.desc}</p>
            </div>
            <Icon name="lucide:arrow-right" width={14} height={14} class="bento-cell-arrow" />
          </a>
        ))}
      </div>

      {/* 开源 + 成本 双列 */}
      <div class="bento-bottom-row">
        <div class="bento-opensource panel-glass">
          <Icon name="lucide:github" width={20} height={20} class="text-brand" />
          <p class="bento-opensource-text">这个站永久免费开源，同更多人同行。</p>
          <a href="https://github.com/AdgaiWalker/AdgaiWalker" target="_blank" rel="noopener noreferrer" class="bento-opensource-link">
            查看 GitHub 仓库
            <Icon name="lucide:external-link" width={12} height={12} />
          </a>
        </div>

        <div class="bento-cost panel-glass">
          <div class="bento-cost-header">
            <span class="bento-cost-label">总装载荷成本</span>
            <span class="bento-cost-total">{totalCost} 元</span>
          </div>
          <div class="bento-cost-grid">
            {costByCategory.map(([cat, amount]) => (
              <div class="bento-cost-item">
                <span class="bento-cost-cat">{cat}</span>
                <span class="bento-cost-amount">{amount} 元</span>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* ===== 5. 工具链 Pipeline ===== */}
  <section class="pipeline-section" id="pipeline-section">
    <div class="about-container">
      <SectionHeader title="战舰装配 · 怎么做的" />

      <div class="pipeline-philosophy panel-glass">
        <p class="pipeline-philosophy-main">因为需要，所以存在。</p>
        <p class="pipeline-philosophy-sub">我不是先选了一个框架，然后看它能做什么。是我先想清楚这个产品要填补我什么需求，然后 AI 根据我的需求推荐框架。</p>
      </div>

      {/* AI 工具矩阵 */}
      <div class="tools-grid">
        {aiTools.map((t) => (
          <div class="tool-card panel-glass">
            <span class="tool-card-dot"></span>
            <p class="tool-card-name">{t.name}</p>
            <p class="tool-card-role">{t.role}</p>
            <span class="tool-card-model">{t.model}</span>
          </div>
        ))}
      </div>

      {/* Pipeline 管线 */}
      <div class="pipeline-flow panel-glass">
        <p class="pipeline-flow-label">星联远征 · 自动装配管线</p>
        <div class="pipeline-nodes">
          <div class="pipeline-node">
            <Icon name="lucide:book-open" width={20} height={20} />
            <span class="pipeline-node-name">Obsidian</span>
            <span class="pipeline-node-role">思想沉淀舱</span>
          </div>
          <div class="pipeline-connector">
            <svg width="100%" height="24" overflow="visible">
              <line x1="0" y1="12" x2="100%" y2="12" stroke="var(--color-nav-border)" stroke-width="1.5" stroke-dasharray="4 4" />
              <line x1="0" y1="12" x2="100%" y2="12" stroke="var(--color-brand)" stroke-width="2" stroke-linecap="round" class="pipeline-pulse-line" />
            </svg>
          </div>
          <div class="pipeline-node">
            <Icon name="lucide:git-compare" width={20} height={20} />
            <span class="pipeline-node-name">GitHub</span>
            <span class="pipeline-node-role">中枢集结港</span>
          </div>
          <div class="pipeline-connector">
            <svg width="100%" height="24" overflow="visible">
              <line x1="0" y1="12" x2="100%" y2="12" stroke="var(--color-nav-border)" stroke-width="1.5" stroke-dasharray="4 4" />
              <line x1="0" y1="12" x2="100%" y2="12" stroke="var(--color-brand)" stroke-width="2" stroke-linecap="round" class="pipeline-pulse-line" />
            </svg>
          </div>
          <div class="pipeline-node">
            <Icon name="lucide:cloud-lightning" width={20} height={20} />
            <span class="pipeline-node-name">Vercel</span>
            <span class="pipeline-node-role">云端启航机</span>
          </div>
        </div>
      </div>

      {/* 开荒星历时间轴 */}
      <div class="site-timeline panel-glass">
        <p class="site-timeline-label">开荒星历时间轴</p>
        <div class="site-timeline-items">
          {siteTimeline.map((t) => (
            <div class="site-timeline-item">
              <span class="site-timeline-date">{t.date}</span>
              <span class="site-timeline-text">{t.text}</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  {/* ===== 6. Footer ===== */}
  <footer class="about-footer" id="about-footer">
    <div class="about-footer-inner">
      <LikeCounter pagePath="/about" />

      <div class="about-uptime panel-glass">
        <Icon name="lucide:hourglass" width={14} height={14} class="uptime-icon" />
        <div class="uptime-text" id="site-uptime"></div>
      </div>

      <a href="#cta-section" class="about-cta-link">
        <Icon name="lucide:arrow-up" width={14} height={14} />
        回到点子共促
      </a>

      <div class="about-signature">
        <span class="about-sig-dot"></span>
        Walker · 行过万里水路
      </div>
    </div>
  </footer>

</FullscreenLayout>


<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  let abort: AbortController | null = null;
  let uptimeTimer: ReturnType<typeof setInterval> | null = null;
  let typingTimer: ReturnType<typeof setTimeout> | null = null;

  function isAboutPage() {
    return !!(document.getElementById('hero-section') && document.querySelector('.about-bio-section'));
  }

  function cleanup() {
    if (!isAboutPage()) return;
    abort?.abort();
    abort = null;
    if (uptimeTimer !== null) {
      clearInterval(uptimeTimer);
      uptimeTimer = null;
    }
    if (typingTimer !== null) {
      clearTimeout(typingTimer);
      typingTimer = null;
    }
    ScrollTrigger.getAll().forEach(t => t.kill());
  }

  /* ---- Typing Effect ---- */
  function initTyping() {
    const el = document.getElementById('hero-typing');
    if (!el) return;
    // Reset state completely
    el.textContent = '';
    el.classList.remove('typing-done');
    if (typingTimer !== null) clearTimeout(typingTimer);

    const text = '用点子连接人与 AI';
    let i = 0;
    const type = () => {
      if (i < text.length) {
        el.textContent = text.slice(0, i + 1);
        i++;
        typingTimer = setTimeout(type, 100 + Math.random() * 60);
      } else {
        el.classList.add('typing-done');
        typingTimer = null;
      }
    };
    typingTimer = setTimeout(type, 800);
  }

  /* ---- Hero Parallax ---- */
  function initParallax(signal: AbortSignal) {
    const hero = document.getElementById('hero-section');
    const bgVideo = hero?.querySelector('.hero-bg-video') as HTMLElement | null;
    const heroContent = hero?.querySelector('.hero-content') as HTMLElement | null;
    const scrollHint = document.getElementById('scroll-hint');

    if (hero) {
      window.addEventListener('scroll', () => {
        const scrollY = window.scrollY;
        const heroHeight = hero.offsetHeight;
        if (scrollY < heroHeight) {
          if (bgVideo) bgVideo.style.transform = `scale(${1 + scrollY / heroHeight * 0.1}) translateY(${scrollY * 0.3}px)`;
          if (heroContent) {
            heroContent.style.opacity = String(1 - scrollY / heroHeight * 1.5);
            heroContent.style.transform = `translateY(${scrollY * 0.15}px)`;
          }
          if (scrollHint) scrollHint.style.opacity = String(Math.max(0, 1 - scrollY / heroHeight * 3));
        }
      }, { passive: true, signal });

      scrollHint?.addEventListener('click', () => {
        document.getElementById('bio-section')?.scrollIntoView({ behavior: 'smooth' });
      }, { signal });
    }
  }

  /* ---- Uptime Counter ---- */
  function initUptimeCounter() {
    const el = document.getElementById('site-uptime');
    if (!el) return;
    const launch = new Date('2026-05-02T00:00:00+08:00');
    const update = () => {
      const diff = Date.now() - launch.getTime();
      const d = Math.floor(diff / 86400000);
      const h = Math.floor((diff % 86400000) / 3600000);
      const m = Math.floor((diff % 3600000) / 60000);
      const s = Math.floor((diff % 60000) / 1000);
      el.textContent = `SYSTEM ONLINE: ${d}D ${h}H ${m}M ${s}S`;
    };
    update();
    uptimeTimer = setInterval(update, 1000);
  }

  /* ---- Email Copy ---- */
  function initEmailCopy(signal: AbortSignal) {
    const btn = document.getElementById('copy-email');
    btn?.addEventListener('click', async () => {
      try {
        await navigator.clipboard.writeText('praxiswalker@gmail.com');
        const span = btn.querySelector('span');
        const orig = span?.textContent;
        if (span) span.textContent = '已复制！';
        setTimeout(() => { if (span && orig) span.textContent = orig; }, 1500);
      } catch { /* fallback */ }
    }, { signal });
  }

  /* ---- GSAP ScrollTrigger Reveals ---- */
  function initScrollReveal() {
    // Bio section
    gsap.fromTo('#bio-section .bio-card',
      { opacity: 0, y: 50 },
      { opacity: 1, y: 0, duration: 0.8, ease: 'power3.out',
        scrollTrigger: { trigger: '#bio-section', start: 'top 80%' }
      }
    );

    // Manifesto
    gsap.fromTo('.manifesto',
      { opacity: 0, y: 40 },
      { opacity: 1, y: 0, duration: 0.7, ease: 'power3.out',
        scrollTrigger: { trigger: '.manifesto', start: 'top 85%' }
      }
    );

    // Timeline items stagger
    gsap.fromTo('.timeline-item',
      { opacity: 0, y: 40, scale: 0.96 },
      { opacity: 1, y: 0, scale: 1, duration: 0.6, ease: 'back.out(1.1)', stagger: 0.12,
        scrollTrigger: { trigger: '.timeline-container', start: 'top 80%' }
      }
    );

    // Why section story
    gsap.fromTo('.why-story',
      { opacity: 0, y: 40 },
      { opacity: 1, y: 0, duration: 0.7, ease: 'power3.out',
        scrollTrigger: { trigger: '.why-story', start: 'top 85%' }
      }
    );

    // Why articles stagger
    gsap.fromTo('.why-article',
      { opacity: 0, y: 25 },
      { opacity: 1, y: 0, duration: 0.5, ease: 'power2.out', stagger: 0.1,
        scrollTrigger: { trigger: '.why-articles-grid', start: 'top 85%' }
      }
    );

    // Timeline progress line
    const progressLine = document.getElementById('timeline-progress');
    if (progressLine) {
      gsap.fromTo(progressLine,
        { scaleY: 0 },
        { scaleY: 1, ease: 'none',
          scrollTrigger: { trigger: '.timeline-container', start: 'top 60%', end: 'bottom 75%', scrub: 0.5 }
        }
      );
    }

    // CTA section
    gsap.fromTo('.cta-card',
      { opacity: 0, y: 50, scale: 0.97 },
      { opacity: 1, y: 0, scale: 1, duration: 0.8, ease: 'power3.out',
        scrollTrigger: { trigger: '#cta-section', start: 'top 80%' }
      }
    );

    // Bento Grid cells stagger
    gsap.fromTo('.bento-cell',
      { opacity: 0, y: 30, scale: 0.95 },
      { opacity: 1, y: 0, scale: 1, duration: 0.5, ease: 'power2.out', stagger: 0.1,
        scrollTrigger: { trigger: '.bento-grid', start: 'top 85%' }
      }
    );

    // Bento bottom row
    gsap.fromTo('.bento-bottom-row > *',
      { opacity: 0, y: 30 },
      { opacity: 1, y: 0, duration: 0.6, ease: 'power2.out', stagger: 0.15,
        scrollTrigger: { trigger: '.bento-bottom-row', start: 'top 85%' }
      }
    );

    // Pipeline nodes
    gsap.fromTo('.pipeline-node',
      { opacity: 0, scale: 0.8 },
      { opacity: 1, scale: 1, duration: 0.5, ease: 'back.out(1.5)', stagger: 0.2,
        scrollTrigger: { trigger: '.pipeline-nodes', start: 'top 85%' }
      }
    );

    // Tools grid
    gsap.fromTo('.tool-card',
      { opacity: 0, y: 20 },
      { opacity: 1, y: 0, duration: 0.4, ease: 'power2.out', stagger: 0.08,
        scrollTrigger: { trigger: '.tools-grid', start: 'top 85%' }
      }
    );
  }

  /* ---- Hero sub-text fade in ---- */
  function initHeroFadeIn() {
    const sub = document.getElementById('hero-sub');
    const era = document.querySelector('.hero-era') as HTMLElement | null;
    const actions = document.getElementById('hero-actions');
    if (sub) gsap.fromTo(sub, { opacity: 0, y: 10 }, { opacity: 1, y: 0, duration: 1.2, delay: 2.2, ease: 'power2.out' });
    if (era) gsap.fromTo(era, { opacity: 0, y: 10 }, { opacity: 1, y: 0, duration: 1.2, delay: 2.8, ease: 'power2.out' });
    if (actions) gsap.fromTo(actions, { opacity: 0, y: 15 }, { opacity: 1, y: 0, duration: 0.8, delay: 3.4, ease: 'power2.out' });
  }

  function initAll() {
    if (!isAboutPage()) return;
    cleanup();
    abort = new AbortController();
    const { signal } = abort;

    initTyping();
    initHeroFadeIn();
    initParallax(signal);
    initUptimeCounter();
    initEmailCopy(signal);
    initScrollReveal();
  }

  initAll();
  document.addEventListener('astro:page-load', initAll);
  document.addEventListener('astro:before-swap', cleanup);
</script>


<style>
  /* ===== 覆盖全局 panel-glass 过白背景 ===== */
  :global(body:not(.theme-aurora):not(.theme-sunset):not(.theme-mint) .about-container .panel-glass) {
    background: rgba(255, 255, 255, 0.25) !important;
    backdrop-filter: blur(16px) !important;
    border-color: rgba(255, 255, 255, 0.5) !important;
  }

  /* ===== 通用容器 ===== */
  .about-container {
    max-width: 680px;
    width: 100%;
    margin: 0 auto;
    padding: 0 1.25rem;
  }

  /* ===== 1. Hero ===== */
  .hero-section {
    position: relative;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-bg-video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform-origin: center;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0.15) 35%, rgba(0,0,0,0.55) 100%);
    backdrop-filter: blur(1px);
  }

  .hero-content {
    position: relative;
    z-index: 10;
    text-align: center;
    color: #fff;
    will-change: opacity, transform;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .hero-avatar-wrapper {
    margin-bottom: 1.5rem;
  }

  .hero-avatar-ring {
    width: 108px;
    height: 108px;
    border-radius: 50%;
    padding: 3px;
    background: linear-gradient(135deg, var(--color-brand), var(--color-brand-secondary, #1fc9e7));
    box-shadow: 0 0 30px rgba(53, 191, 171, 0.3), 0 0 60px rgba(53, 191, 171, 0.1);
    animation: ring-breathe 3s ease-in-out infinite;
  }

  @keyframes ring-breathe {
    0%, 100% { box-shadow: 0 0 30px rgba(53, 191, 171, 0.25), 0 0 60px rgba(53, 191, 171, 0.08); }
    50% { box-shadow: 0 0 40px rgba(53, 191, 171, 0.4), 0 0 80px rgba(53, 191, 171, 0.15); }
  }

  .hero-avatar-img {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    object-fit: cover;
    display: block;
  }

  .hero-title {
    font-family: 'Sora', sans-serif;
    font-size: clamp(3rem, 9vw, 5.5rem);
    font-weight: 700;
    letter-spacing: -0.04em;
    line-height: 1;
    margin: 0;
    text-shadow: 0 4px 30px rgba(0,0,0,0.35);
  }

  .hero-typing {
    font-family: 'Noto Sans SC', 'PingFang SC', sans-serif;
    font-size: clamp(0.95rem, 2.5vw, 1.3rem);
    font-weight: 300;
    color: rgba(255,255,255,0.85);
    letter-spacing: 0.15em;
    margin: 0.5rem 0 0;
    min-height: 1.6em;
    border-right: 2px solid rgba(255,255,255,0.7);
    padding-right: 2px;
    animation: blink-cursor 0.8s step-end infinite;
  }

  .hero-typing.typing-done {
    border-right-color: transparent;
    animation: none;
  }

  @keyframes blink-cursor {
    0%, 100% { border-right-color: rgba(255,255,255,0.7); }
    50% { border-right-color: transparent; }
  }

  .hero-sub {
    font-size: clamp(0.72rem, 1.5vw, 0.9rem);
    color: rgba(255,255,255,0.45);
    letter-spacing: 0.06em;
    margin: 0.5rem 0 0;
    opacity: 0;
  }

  .hero-era {
    font-size: clamp(0.68rem, 1.3vw, 0.82rem);
    color: rgba(255,255,255,0.3);
    letter-spacing: 0.04em;
    margin: 0.25rem 0 0;
    max-width: 440px;
    line-height: 1.6;
    opacity: 0;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-top: 1.25rem;
    opacity: 0;
  }

  .hero-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.6rem 1.5rem;
    border-radius: 999px;
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    cursor: pointer;
  }

  .hero-btn--primary {
    background: var(--color-brand);
    color: #fff;
    border: 1px solid var(--color-brand);
    box-shadow: 0 4px 20px rgba(53, 191, 171, 0.3);
  }

  .hero-btn--primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 30px rgba(53, 191, 171, 0.45);
  }

  .hero-btn--ghost {
    background: rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.85);
    border: 1px solid rgba(255, 255, 255, 0.25);
    backdrop-filter: blur(12px);
  }

  .hero-btn--ghost:hover {
    background: rgba(255, 255, 255, 0.2);
    color: #fff;
    transform: translateY(-2px);
  }

  .hero-scroll-hint {
    position: absolute;
    bottom: 3.5rem;
    left: 50%;
    transform: translateX(-50%);
    z-index: 10;
    color: rgba(255,255,255,0.5);
    cursor: pointer;
    animation: bounce-down 2s ease-in-out infinite;
    transition: opacity 0.3s ease;
  }

  @keyframes bounce-down {
    0%, 100% { transform: translateX(-50%) translateY(0); }
    50% { transform: translateX(-50%) translateY(8px); }
  }

  .hero-gradient-bar {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 80px;
    background: linear-gradient(to top, var(--color-bg), transparent);
    z-index: 5;
    pointer-events: none;
  }

  /* ===== 2. Bio ===== */
  .about-bio-section {
    padding: 5rem 0 4rem;
  }

  .bio-card {
    padding: 2rem 2rem 1.5rem;
    margin-top: 1.5rem;
  }

  .bio-narrative {
    font-size: 0.9rem;
    line-height: 1.9;
    color: var(--color-parchment);
    opacity: 0.8;
  }

  .bio-narrative p {
    margin: 0 0 0.75rem;
  }

  .bio-quote {
    position: relative;
    padding: 1.25rem 1.5rem;
    border-radius: 16px;
    border: 1px solid var(--color-nav-border);
    background: var(--color-brand-glow);
    margin: 1.5rem 0;
    overflow: hidden;
    text-align: center;
  }

  .bio-quote-mark {
    position: absolute;
    right: 1rem;
    bottom: -0.5rem;
    font-size: 5rem;
    font-family: Georgia, serif;
    color: var(--color-brand);
    opacity: 0.06;
    pointer-events: none;
    user-select: none;
  }

  .bio-quote-text {
    font-family: var(--font-heading);
    font-size: 0.875rem;
    font-weight: 700;
    font-style: italic;
    color: var(--color-parchment);
    line-height: 1.7;
    margin: 0;
  }

  .bio-quote-author {
    font-size: 0.56rem;
    font-family: var(--font-mono);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-parchment-dim);
    opacity: 0.5;
    margin: 0.5rem 0 0;
  }

  .bio-gratitude {
    position: relative;
    padding: 1.25rem 1.5rem;
    border-radius: 16px;
    border-left: 3px solid var(--color-brand) !important;
    margin: 1.5rem 0;
    overflow: hidden;
  }

  .bio-gratitude-stamp {
    position: absolute;
    top: 0.75rem;
    right: 1rem;
    border: 2px dashed rgba(53, 191, 171, 0.3);
    padding: 2px 6px;
    font-size: 0.56rem;
    color: var(--color-brand);
    opacity: 0.4;
    font-family: var(--font-mono);
    letter-spacing: 0.15em;
    user-select: none;
  }

  .bio-gratitude-title {
    font-size: 0.875rem;
    font-weight: 700;
    color: var(--color-brand);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin: 0 0 0.75rem;
  }

  .bio-gratitude-text {
    font-size: 0.78rem;
    line-height: 1.7;
    color: var(--color-parchment);
    opacity: 0.65;
    font-style: italic;
    margin: 0;
  }

  .bio-social {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.6rem;
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-nav-border);
  }

  .social-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.45rem 1rem;
    border-radius: 999px;
    border: 1px solid var(--color-nav-border);
    background: rgba(255,255,255,0.04);
    font-size: 0.82rem;
    font-weight: 500;
    color: var(--color-parchment-dim);
    text-decoration: none;
    transition: all 0.25s ease;
    cursor: pointer;
  }

  .social-pill:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.08);
    background: #000;
    color: #fff;
    border-color: #000;
  }

  .social-pill--bili:hover { background: #fb7299; border-color: #fb7299; }
  .social-pill--xhs:hover { background: #ff2442; border-color: #ff2442; }
  .social-pill--email:hover { background: var(--color-brand); border-color: var(--color-brand); }

  /* ===== 3. Journey / Timeline ===== */
  .journey-section {
    padding: 4rem 0 5rem;
  }

  .manifesto {
    padding: 1.5rem;
    margin-top: 1.5rem;
    margin-bottom: 3rem;
    overflow: hidden;
  }

  .manifesto-inner {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
  }

  .manifesto-icon {
    width: 40px;
    height: 40px;
    border-radius: 12px;
    background: rgba(53, 191, 171, 0.1);
    border: 1px solid rgba(53, 191, 171, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-brand);
    flex-shrink: 0;
  }

  .manifesto-text {
    font-family: serif;
    font-size: 0.95rem;
    font-style: italic;
    font-weight: 500;
    color: var(--color-parchment);
    line-height: 1.7;
    letter-spacing: 0.03em;
    margin: 0;
  }

  .manifesto-source {
    font-size: 0.56rem;
    font-family: var(--font-mono);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-parchment-dim);
    opacity: 0.5;
    margin: 0.5rem 0 0;
  }

  .manifesto-states {
    display: flex;
    align-items: center;
    justify-content: space-around;
    background: rgba(0,0,0,0.03);
    border: 1px solid var(--color-nav-border);
    border-radius: 16px;
    padding: 0.6rem;
    margin-top: 1rem;
  }

  :global(body.theme-aurora) .manifesto-states,
  :global(body.theme-sunset) .manifesto-states,
  :global(body.theme-mint) .manifesto-states {
    background: rgba(255,255,255,0.03);
  }

  .manifesto-state {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.68rem;
    font-weight: 600;
    color: var(--color-parchment-dim);
    opacity: 0.55;
    padding: 0.35rem 0.75rem;
    border-radius: 12px;
  }

  .manifesto-state--active {
    opacity: 1;
    color: var(--color-brand);
    font-weight: 800;
    font-size: 0.72rem;
    background: rgba(53, 191, 171, 0.1);
    border: 1px solid rgba(53, 191, 171, 0.2);
    box-shadow: 0 2px 8px rgba(53, 191, 171, 0.1);
  }

  .state-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
  }

  .state-dot--amber { background: #f59e0b; opacity: 0.6; }
  .state-dot--brand { background: var(--color-brand); animation: pulse-dot 2s ease-in-out infinite; }
  .state-dot--emerald { background: #34d399; opacity: 0.6; }

  @keyframes pulse-dot {
    0%, 100% { opacity: 0.6; }
    50% { opacity: 1; box-shadow: 0 0 8px rgba(53, 191, 171, 0.5); }
  }

  /* Timeline */
  .timeline-container {
    position: relative;
    padding: 1rem 0;
  }

  .timeline-track {
    position: absolute;
    left: 1rem;
    top: 0;
    bottom: 0;
    width: 2px;
    background: var(--color-nav-border);
  }

  .timeline-progress {
    position: absolute;
    left: 1rem;
    top: 0;
    bottom: 0;
    width: 3px;
    transform-origin: top;
    transform: scaleY(0);
    background: linear-gradient(to bottom, var(--color-brand) 70%, var(--color-brand-secondary, #1fc9e7));
    box-shadow: 0 0 12px var(--color-brand);
    border-radius: 99px;
    z-index: 2;
  }

  .timeline-item {
    position: relative;
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding-left: 2.5rem;
    margin-bottom: 2rem;
  }

  .timeline-dot {
    position: absolute;
    left: 0.35rem;
    top: 0.3rem;
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: var(--color-bg);
    border: 2px solid var(--color-brand);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 5;
    box-shadow: 0 0 10px var(--color-brand-glow);
  }

  .timeline-dot-core {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--color-brand);
  }

  .timeline-dot-ripple {
    position: absolute;
    inset: 0;
    border-radius: 50%;
    border: 1px solid rgba(53, 191, 171, 0.4);
    animation: ripple-out 2s cubic-bezier(0.16, 1, 0.3, 1) infinite;
  }

  @keyframes ripple-out {
    0% { transform: scale(1); opacity: 0.7; }
    100% { transform: scale(2.2); opacity: 0; }
  }

  .timeline-date {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--color-brand);
    background: rgba(53, 191, 171, 0.1);
    padding: 0.15rem 0.6rem;
    border-radius: 999px;
    white-space: nowrap;
    flex-shrink: 0;
    margin-top: 0.15rem;
  }

  .timeline-card-col {
    flex: 1;
    min-width: 0;
  }

  .timeline-card {
    padding: 1rem 1.25rem;
    border-radius: 16px !important;
    transition: border-color 0.3s ease !important;
  }

  .timeline-card:hover {
    border-color: rgba(53, 191, 171, 0.35) !important;
    box-shadow: 0 4px 12px rgba(53, 191, 171, 0.08);
  }

  .timeline-card-title {
    font-size: 0.85rem;
    font-weight: 700;
    margin: 0 0 0.4rem;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--color-parchment);
  }

  .timeline-card-title [data-icon] {
    color: var(--color-brand);
  }

  .timeline-card-desc {
    font-size: 0.78rem;
    color: var(--color-parchment);
    opacity: 0.65;
    line-height: 1.6;
    margin: 0;
  }

  .timeline-children {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .timeline-child {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    font-size: 0.78rem;
    color: var(--color-parchment);
    opacity: 0.65;
    line-height: 1.6;
  }

  .timeline-child-label {
    color: var(--color-brand);
    font-weight: 700;
    flex-shrink: 0;
    text-decoration: none;
  }

  a.timeline-child-label:hover {
    text-decoration: underline;
  }

  /* Desktop: alternate sides */
  @media (min-width: 640px) {
    .timeline-track {
      left: 50%;
      transform: translateX(-50%);
    }
    .timeline-progress {
      left: 50%;
      transform: translateX(-50%) scaleY(0);
    }
    .timeline-item {
      padding-left: 0;
      width: 100%;
      justify-content: flex-end;
    }
    .timeline-dot {
      left: 50%;
      transform: translateX(-50%);
    }
    .timeline-date-col {
      position: absolute;
      left: 0;
      width: 45%;
      text-align: right;
      padding-right: 2rem;
    }
    .timeline-card-col {
      width: 45%;
      margin-left: 55%;
    }
    .timeline-item--right .timeline-date-col {
      left: auto;
      right: 0;
      width: 45%;
      text-align: left;
      padding-right: 0;
      padding-left: 2rem;
    }
    .timeline-item--right .timeline-card-col {
      width: 45%;
      margin-left: 0;
      margin-right: 55%;
    }
  }

  /* ===== 4. Bento Grid ===== */
  .bento-section {
    padding: 4rem 0 5rem;
  }

  .bento-motivation {
    padding: 1.5rem 2rem;
    margin-top: 1.5rem;
    margin-bottom: 2rem;
    text-align: center;
    font-size: 0.9rem;
    line-height: 1.8;
    color: var(--color-parchment);
    opacity: 0.75;
  }

  .bento-motivation strong {
    color: var(--color-brand);
    font-weight: 700;
  }

  .bento-motivation p {
    margin: 0 0 0.3rem;
  }

  .bento-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .bento-cell {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1.1rem 1rem;
    border-radius: 20px !important;
    text-decoration: none;
    color: var(--color-parchment);
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1) !important;
  }

  .bento-cell::before {
    display: none;
  }

  .bento-cell:hover::before { display: none; }
  .bento-cell:hover {
    transform: translateY(-2px);
    border-color: rgba(53, 191, 171, 0.35) !important;
    box-shadow: 0 4px 16px rgba(53, 191, 171, 0.1);
  }

  .bento-cell-icon {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    background: rgba(53, 191, 171, 0.1);
    border: 1px solid rgba(53, 191, 171, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-brand);
    flex-shrink: 0;
  }

  .bento-cell-body {
    flex: 1;
    min-width: 0;
  }

  .bento-cell-header {
    display: flex;
    align-items: baseline;
    gap: 0.4rem;
    margin-bottom: 0.15rem;
  }

  .bento-cell-label {
    font-size: 0.85rem;
    font-weight: 700;
  }

  .bento-cell-hint {
    font-size: 0.56rem;
    font-family: var(--font-mono);
    text-transform: uppercase;
    color: var(--color-parchment-dim);
    opacity: 0.4;
  }

  .bento-cell-desc {
    font-size: 0.72rem;
    color: var(--color-parchment);
    opacity: 0.55;
    line-height: 1.5;
    margin: 0;
  }

  .bento-cell-arrow {
    color: var(--color-parchment-dim);
    opacity: 0.3;
    flex-shrink: 0;
    transition: opacity 0.3s, transform 0.3s;
  }

  .bento-cell:hover .bento-cell-arrow {
    opacity: 0.8;
    transform: translateX(3px);
    color: var(--color-brand);
  }

  @media (max-width: 480px) {
    .bento-grid { grid-template-columns: 1fr; }
  }

  .bento-bottom-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
    margin-bottom: 2rem;
  }

  @media (max-width: 560px) {
    .bento-bottom-row { grid-template-columns: 1fr; }
  }

  .bento-opensource {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 0.75rem;
  }

  .bento-opensource-text {
    font-family: var(--font-heading);
    font-size: 0.85rem;
    font-weight: 500;
    font-style: italic;
    color: var(--color-parchment);
    line-height: 1.6;
    margin: 0;
  }

  .bento-opensource-link {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    font-size: 0.78rem;
    color: var(--color-brand);
    text-decoration: none;
    font-weight: 500;
  }

  .bento-opensource-link:hover { text-decoration: underline; }

  .bento-cost {
    padding: 1.25rem;
  }

  .bento-cost-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 0.75rem;
  }

  .bento-cost-label {
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--color-parchment);
  }

  .bento-cost-total {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-brand);
  }

  .bento-cost-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.4rem;
  }

  .bento-cost-item {
    text-align: center;
  }

  .bento-cost-cat {
    display: block;
    font-size: 0.62rem;
    color: var(--color-parchment-dim);
    opacity: 0.5;
  }

  .bento-cost-amount {
    display: block;
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--color-parchment);
  }

  /* ===== Why Section ===== */
  .why-section {
    padding: 4rem 0 4rem;
  }

  .why-story {
    padding: 1.75rem 2rem;
    margin-top: 1.5rem;
    margin-bottom: 2rem;
  }

  .why-story p {
    font-size: 0.88rem;
    line-height: 1.9;
    color: var(--color-parchment);
    opacity: 0.78;
    margin: 0 0 0.75rem;
  }

  .why-story p:last-child {
    margin-bottom: 0;
  }

  .why-articles-hint {
    font-size: 0.72rem;
    text-align: center;
    color: var(--color-parchment-dim);
    opacity: 0.4;
    margin-bottom: 0.75rem;
  }

  .why-articles-grid {
    display: grid;
    gap: 0.6rem;
  }

  .why-article {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    padding: 1rem 1.25rem;
    border-radius: 16px !important;
    text-decoration: none;
    color: var(--color-parchment);
    transition: border-color 0.3s ease !important;
  }

  .why-article:hover {
    border-color: rgba(53, 191, 171, 0.35) !important;
    box-shadow: 0 4px 12px rgba(53, 191, 171, 0.08);
  }

  .why-article [data-icon] {
    flex-shrink: 0;
    margin-top: 2px;
  }

  .why-article-title {
    font-size: 0.85rem;
    font-weight: 700;
    margin: 0 0 0.25rem;
    transition: color 0.2s;
  }

  .why-article:hover .why-article-title {
    color: var(--color-brand);
  }

  .why-article-summary {
    font-size: 0.72rem;
    color: var(--color-parchment);
    opacity: 0.55;
    line-height: 1.5;
    margin: 0;
  }

  /* ===== 5. Pipeline ===== */
  .pipeline-section {
    padding: 4rem 0 5rem;
  }

  .pipeline-philosophy {
    padding: 1.5rem 2rem;
    margin-top: 1.5rem;
    margin-bottom: 2rem;
    border-left: 3px solid var(--color-brand) !important;
  }

  .pipeline-philosophy-main {
    font-family: var(--font-heading);
    font-size: 0.95rem;
    font-weight: 500;
    font-style: italic;
    color: var(--color-parchment);
    margin: 0;
  }

  .pipeline-philosophy-sub {
    font-size: 0.82rem;
    color: var(--color-parchment);
    opacity: 0.55;
    line-height: 1.7;
    margin: 0.75rem 0 0;
  }

  .tools-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.6rem;
    margin-bottom: 1.5rem;
  }

  @media (max-width: 480px) {
    .tools-grid { grid-template-columns: repeat(2, 1fr); }
  }

  .tool-card {
    padding: 0.9rem;
    text-align: center;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease !important;
  }

  .tool-card:hover {
    border-color: rgba(53, 191, 171, 0.35) !important;
    box-shadow: 0 4px 12px rgba(53, 191, 171, 0.08);
  }

  .tool-card-dot {
    position: absolute;
    top: 0.5rem;
    right: 0.5rem;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--color-brand);
    opacity: 0.3;
    transition: opacity 0.3s;
  }

  .tool-card:hover .tool-card-dot {
    opacity: 1;
    animation: pulse-dot 1.5s ease-in-out infinite;
  }

  .tool-card-name {
    font-size: 0.78rem;
    font-weight: 700;
    color: var(--color-parchment);
    margin: 0;
    transition: color 0.2s;
  }

  .tool-card:hover .tool-card-name {
    color: var(--color-brand);
  }

  .tool-card-role {
    font-size: 0.62rem;
    color: var(--color-parchment-dim);
    opacity: 0.5;
    margin: 0.25rem 0 0;
  }

  .tool-card-model {
    display: inline-block;
    font-size: 0.56rem;
    font-family: var(--font-mono);
    font-weight: 700;
    color: var(--color-brand);
    border: 1px solid rgba(53, 191, 171, 0.2);
    background: rgba(53, 191, 171, 0.05);
    padding: 0.15rem 0.5rem;
    border-radius: 4px;
    margin-top: 0.5rem;
    transform: scale(0.9);
  }

  .pipeline-flow {
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .pipeline-flow-label {
    font-size: 0.62rem;
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-brand);
    margin: 0 0 1rem;
  }

  .pipeline-nodes {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0;
  }

  .pipeline-node {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.35rem;
    padding: 0.75rem 1rem;
    border-radius: 12px;
    border: 1px solid rgba(53, 191, 171, 0.2);
    background: rgba(53, 191, 171, 0.05);
    color: var(--color-brand);
    min-width: 90px;
    text-align: center;
    z-index: 2;
    transition: transform 0.3s ease;
  }

  .pipeline-node:hover {
    transform: scale(1.05);
  }

  .pipeline-node-name {
    font-size: 0.78rem;
    font-weight: 700;
    color: var(--color-parchment);
  }

  .pipeline-node-role {
    font-size: 0.5rem;
    font-family: var(--font-mono);
    text-transform: uppercase;
    color: var(--color-parchment-dim);
    opacity: 0.4;
  }

  .pipeline-connector {
    flex: 1;
    min-width: 30px;
  }

  @keyframes pipeline-flow {
    0% { stroke-dashoffset: 24; }
    100% { stroke-dashoffset: 0; }
  }

  .pipeline-pulse-line {
    stroke-dasharray: 8 16;
    animation: pipeline-flow 1.5s linear infinite;
  }

  @media (max-width: 480px) {
    .pipeline-nodes { flex-direction: column; gap: 0.5rem; }
    .pipeline-connector { min-height: 24px; min-width: auto; transform: rotate(90deg); }
    .pipeline-node { min-width: 80%; }
  }

  .site-timeline {
    padding: 1.5rem;
  }

  .site-timeline-label {
    font-size: 0.62rem;
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-brand);
    margin: 0 0 1rem;
  }

  .site-timeline-items {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .site-timeline-item {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
  }

  .site-timeline-date {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--color-brand);
    background: rgba(53, 191, 171, 0.1);
    padding: 0.1rem 0.5rem;
    border-radius: 999px;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }

  .site-timeline-text {
    font-size: 0.82rem;
    color: var(--color-parchment);
    opacity: 0.65;
  }

  /* ===== CTA Section ===== */
  .cta-section {
    padding: 4rem 0 4rem;
  }

  .cta-card {
    border-radius: 24px;
    background: linear-gradient(135deg, rgba(53, 191, 171, 0.08), rgba(53, 191, 171, 0.02));
    border: 1.5px solid rgba(53, 191, 171, 0.2);
    padding: 2.5rem;
    box-shadow: 0 8px 40px rgba(53, 191, 171, 0.06);
    overflow: hidden;
    position: relative;
  }

  .cta-card::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 300px;
    height: 300px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(53, 191, 171, 0.08) 0%, transparent 70%);
    pointer-events: none;
  }

  .cta-card-inner {
    display: flex;
    align-items: flex-start;
    gap: 2rem;
    position: relative;
    z-index: 1;
  }

  .cta-text-col {
    flex: 1;
    min-width: 0;
  }

  .cta-headline {
    font-family: var(--font-heading);
    font-size: 1.35rem;
    font-weight: 800;
    color: var(--color-parchment);
    margin: 0 0 0.35rem;
    letter-spacing: -0.02em;
  }

  .cta-philosophy {
    font-size: 0.95rem;
    font-weight: 700;
    color: var(--color-brand);
    letter-spacing: 0.1em;
    margin: 0 0 0.75rem;
  }

  .cta-desc {
    font-size: 0.85rem;
    line-height: 1.8;
    color: var(--color-parchment);
    opacity: 0.7;
    margin: 0 0 1rem;
  }

  .cta-highlights {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    margin-bottom: 1.5rem;
  }

  .cta-highlight {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    font-size: 0.82rem;
    color: var(--color-parchment);
    opacity: 0.75;
    line-height: 1.5;
  }

  .cta-highlight [data-icon] {
    color: var(--color-brand);
    flex-shrink: 0;
    margin-top: 2px;
  }

  .cta-actions {
    display: flex;
    align-items: center;
    gap: 1.25rem;
  }

  .cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.6rem 1.5rem;
    border-radius: 999px;
    font-size: 0.85rem;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    cursor: pointer;
  }

  .cta-btn--primary {
    background: var(--color-brand);
    color: #fff;
    border: 1px solid var(--color-brand);
    box-shadow: 0 4px 16px rgba(53, 191, 171, 0.25);
  }

  .cta-btn--primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 24px rgba(53, 191, 171, 0.4);
  }

  .cta-social-row {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .cta-social-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 34px;
    height: 34px;
    border-radius: 50%;
    border: 1px solid var(--color-nav-border);
    color: var(--color-parchment-dim);
    text-decoration: none;
    transition: all 0.25s ease;
  }

  .cta-social-icon:hover {
    background: var(--color-brand);
    border-color: var(--color-brand);
    color: #fff;
    transform: scale(1.1);
  }

  .cta-qr-col {
    flex-shrink: 0;
  }

  .cta-qr-wrapper {
    text-align: center;
  }

  .cta-qr-img {
    width: 140px;
    height: 140px;
    border-radius: 16px;
    border: 2px solid rgba(53, 191, 171, 0.15);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
    object-fit: cover;
  }

  .cta-qr-label {
    font-size: 0.68rem;
    font-family: var(--font-mono);
    color: var(--color-parchment-dim);
    opacity: 0.5;
    margin: 0.5rem 0 0;
  }

  @media (max-width: 640px) {
    .cta-card { padding: 1.75rem; }
    .cta-card-inner { flex-direction: column; align-items: center; text-align: center; }
    .cta-text-col { display: flex; flex-direction: column; align-items: center; }
    .cta-highlights { align-items: flex-start; text-align: left; }
    .cta-actions { flex-direction: column; gap: 0.75rem; }
    .cta-qr-col { order: -1; }
  }

  /* ===== 6. Footer ===== */
  .about-footer {
    padding: 4rem 1rem 3rem;
    border-top: 1px solid var(--color-nav-border);
    background: rgba(255,255,255,0.02);
  }

  .about-footer-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.25rem;
    max-width: 620px;
    margin: 0 auto;
    text-align: center;
  }

  .about-uptime {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.5rem 1.25rem;
    border-radius: 999px;
    border: 1px solid rgba(53, 191, 171, 0.2) !important;
    background: rgba(53, 191, 171, 0.05) !important;
  }

  .uptime-icon {
    color: var(--color-brand);
    animation: spin 4s linear infinite;
  }

  @keyframes spin {
    100% { transform: rotate(360deg); }
  }

  .uptime-text {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.05em;
    color: var(--color-parchment);
    opacity: 0.75;
    font-variant-numeric: tabular-nums;
  }

  .about-cta-text {
    font-size: 0.82rem;
    color: var(--color-parchment);
    opacity: 0.45;
    margin: 0;
  }

  .about-cta-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--color-brand);
    text-decoration: none;
  }

  .about-cta-link:hover { text-decoration: underline; }

  .about-signature {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.62rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-parchment-dim);
    margin-top: 0.5rem;
  }

  .about-sig-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--color-brand);
    animation: pulse-dot 2s ease-in-out infinite;
  }

  /* ===== Responsive ===== */
  @media (max-width: 640px) {
    .hero-title { font-size: 2.6rem; }
    .about-bio-section,
    .journey-section,
    .bento-section,
    .pipeline-section {
      padding-top: 3rem;
      padding-bottom: 3rem;
    }
    .bio-card { padding: 1.5rem; }
    .timeline-dot { left: 0.35rem !important; transform: none !important; }
  }

  /* ===== 右上角导航增强可见度 ===== */
  :global(.fullscreen-nav) {
    gap: 0.35rem;
  }

  :global(.fullscreen-nav-btn) {
    background: rgba(0, 0, 0, 0.45) !important;
    backdrop-filter: blur(20px) saturate(180%) !important;
    -webkit-backdrop-filter: blur(20px) saturate(180%) !important;
    border: 1px solid rgba(255, 255, 255, 0.18) !important;
    color: rgba(255, 255, 255, 0.9) !important;
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.25) !important;
    font-weight: 600;
  }

  :global(.fullscreen-nav-btn:hover) {
    background: rgba(0, 0, 0, 0.65) !important;
    color: #fff !important;
    border-color: rgba(255, 255, 255, 0.3) !important;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.35) !important;
  }
</style>
