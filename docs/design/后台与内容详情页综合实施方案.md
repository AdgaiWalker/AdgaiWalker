# Walker 后台与内容详情页综合实施方案

Status: In progress / UX0 accepted, S0 + UX1 productionization next
Updated: 2026-06-21
Scope: AdgaiWalker 个人系统  
Owner: Walker

> 本文统一后台决策系统与内容详情页的模块责任、数据关系、实施顺序和验收标准。它是候选实施方案，不代表其中功能已经实现。选定的阶段进入 `walker-northstar/references/working/` 工作区后，才拥有当前执行权。

## 1. 核心结论

后台与内容详情页不是两个并列项目。

- 后台负责把需求、主张、反馈和系统事件变成决定与行动。
- 内容详情页负责交付内容，并采集访客是否真的解决了问题。
- 结果数据回到后台，用于决定修改内容、补教程、改功能、停止维护或沉淀资产。

统一产品契约：

```text
Evidence
-> Decision
-> Action
-> Published Result
-> Outcome
-> Capability
-> New Evidence
```

### 1.1 当前实施判断

截至 2026-06-21，P0—P2 的主要数据契约、API、状态机和自动测试已经建立；UX0 已把工作台、事项详情、内容反馈、Outcome、资产学习任务和开发演示场景串成可见的作者操作路径。

当前剩余问题不是缺少更多后台页面，而是两个边界还没有完成生产级证明：

```text
S0 生产真实性：Redis 缺失、重启持久已有代码层门闩与验证脚本；当前环境缺 Redis 凭据，生产等价验证未通过；高风险审计失败关闭已完成代码门闩
UX1 生产化：主题与媒体元数据 Redis 路径已恢复；媒体文件本体已从 Data URL 迁出，并改为生产使用 Vercel Blob、开发/测试使用本地文件；验证脚本已切到真实 Blob 写入、读回、跨进程读回、大文件视频 multipart、私有 URL、删除和删除后不可读；当前环境缺 Blob token，生产等价验证未通过
```

因此暂停新增能力扩张。当前最高优先级是 `S0` 剩余生产真实性阻断，其次是 `UX1` 生产级主题与媒体；完整遥测、AI 自动化、Contributor 和 NorthStar 继续后置。

## 2. 优先级评判规则

不按页面数量、视觉完成度或开发方便程度排序。统一按以下顺序判断：

```text
0. 是否存在生产安全、权限、持久化或隐私阻断
1. 作者能否在 10 秒内判断现在最该处理什么
2. 作者能否在一个连续界面中完成判断、行动和验证
3. 数据是否真实、持久、可追溯
4. 操作后是否立即产生可见、可解释的状态变化
5. 是否能产生可验证结果并进入下一次决定
6. 是否解除后续模块的共享依赖
7. 是否减少重复工作并沉淀能力
8. 最后才是完整遥测、自动化协作、公共网络和交易
```

“观感增强”不再与“体验”混为一类：信息层级、交互连续性、状态反馈属于核心产品能力；主题、背景和媒体个性化属于 `UX1`。

不同性质的事项先分队列，再在同类队列内排序：

| 队列 | 判断依据 |
| --- | --- |
| 用户需求 | 影响范围、频率、原话证据、时效、未解决程度、行动成本 |
| Walker 主张 | 长期方向、个人承诺、学习价值、当前时机、可验证性 |
| 系统事件 | 安全、隐私、可用性、波及范围、紧急度、可逆性 |
| AI 资产 | 复用频率、结果稳定性、边界缺口、错误风险、补证成本 |

## 3. 模块责任

### 3.1 一级模块

| 模块 | 拥有的责任 | 拥有的事实 | 不应负责 |
| --- | --- | --- | --- |
| 工作台 | 汇总待决定、待执行、待验证和已阻塞事项 | 不拥有原始事实；只保存决定、行动及其状态 | 复制用户、内容、反馈或系统数据 |
| 需求 | 管理用户、需求原话、反馈、Walker 主张、需求簇和选题 | `NeedCase`、`Feedback`、`WalkerThesis`、`TopicCandidate` | 编辑或发布内容 |
| 创作 | 管理简报、文章、功能改动、发布状态与内容命中 | `Content`、`Brief`、发布记录及内容关联 | 定义需求的原始身份 |
| 资产 | 管理知识来源、真实经验、规则、Skill、学习任务和 Agent 调用 | `KnowledgeSource`、`Experience`、`Rule`、`Skill`、`LearningRequest` | 把未验证材料直接注册为 Skill |
| 系统 | 管理登录、权限、Gateway、遥测、事件、日志、主题和媒体 | 运行状态、授权、审计、配置和媒体元数据 | 决定创作方向 |

### 3.2 内容详情页的责任

内容详情页是创作模块的公开交付面，同时是 Outcome 的采集入口，不是新的后台一级模块。

| 部件 | 责任 | 不负责 |
| --- | --- | --- |
| `ContentShell` | 阅读容器、TOC、进度、反馈区、评论区 | 探测首块类型或替内容块决定宽度 |
| 内容 Block | 表达视频、提示词、对话、步骤、资源等局部语义 | 保存后台决定 |
| `BlockFeedback` | 提交“有用 / 需补充 / 已过时”等明确信号 | 自行计算命中率 |
| `BlockLike` | 采集轻量喜欢信号 | 代替“问题是否解决”的判断 |
| Feedback API | 验证、脱敏、持久化反馈，建立内容与来源关联 | 在浏览器本地冒充持久化 |
| hit-rate | 从真实反馈和来源关联派生结果 | 拥有第二份反馈事实 |

### 3.3 跨模块共享对象

| 对象 | 用途 |
| --- | --- |
| `EvidenceRef` | 指向原始需求、反馈、事件、内容或调用结果，不复制原文 |
| `DecisionItem` | 记录当时要决定什么、依据什么以及 Walker 的选择 |
| `ActionItem` | 记录由谁执行什么、预期结果、验证时间和回滚方式 |
| `OutcomeEvent` | 记录现实结果、证据和对原决定的修正 |
| `LearningRequest` | 证据不足时，要求 Walker 补充实践、反例、访谈或结果 |

## 4. 模块关系

### 4.1 需求到内容结果

```text
访客访问 / 搜索 / 提问 / 反馈
        -> EvidenceRef
        -> NeedCase / Feedback
        -> TopicCandidate
        -> DecisionItem
        -> ActionItem
        -> Brief / Content / Feature Change
        -> 发布到内容详情页
        -> 明确反馈 + 必要遥测
        -> OutcomeEvent
        -> 修改 / 维持 / 停止 / 补证 / 资产候选
```

### 4.2 Walker 主张与用户需求

```text
WalkerThesis --------------------\
                                  -> converged TopicCandidate
NeedCase / Feedback -------------/
```

- 两边可以汇合，但原始对象不删除。
- `demandSourceIds` 与 `walkerThesisId` 分别保留。
- Walker 主张不得写成“用户高频需求”。
- 后续 Outcome 可同时回写两边，但不改变其来源身份。

### 4.3 结果到资产

```text
OutcomeEvent
  -> 一次有效：Experience / case
  -> 重复有效：Rule candidate
  -> 边界清晰、输出可验证：Skill candidate
  -> Walker 批准后：registered Skill
```

AI 可以生成候选、评测和缺口分析，不能自行将未验证材料升级为 Walker 能力。

## 5. 真实数据契约

每条进入后台的事实或派生判断至少带有：

```text
sourceType
sourceId
occurredAt
collectedAt
environment
visibility
consent / lawfulBasis
freshness
qualityStatus
```

真实性规则：

1. 数据缺失显示“未采集 / 无法判断”，不用静态数字补位。
2. AI 推断使用 `proposal` 或 `hypothesis` 身份，不冒充事实。
3. 生产数据必须持久化；进程内存只能用于明确标记的开发模式。
4. `localStorage` 可记录 TOC 折叠、主题偏好等客户端偏好，不保存决定、行动、反馈或 Outcome 的权威状态。
5. 工作台、hit-rate 和数据分析只做投影，不建立新的原始事实库。
6. 每项数据必须能显示来源、时间、统计范围和更新状态。

## 6. 综合实施顺序

### H0：已知问题快速收口

目标：清除内容详情页已经确认的功能错误和单一真相源问题。

| ID | 任务 | 依赖 | 验收 |
| --- | --- | --- | --- |
| H0-01 | 把 TOC 自动滚动容器从 `.ghost-toc-inner` 修正为 `.toc-sidebar-inner` | 无 | 活跃目录项会自动进入可视区 |
| H0-02 | 删除 `.cs-layout` 和 `.toc-fab` 死 CSS | 无 | 全局零引用，视觉无变化 |
| H0-03 | 用 `--cs-toc-offset` 取代两处 `362px` | H0-02 | TOC 位置等价 |
| H0-04 | 抽取 `setCollapsed()` | 无 | 折叠、展开和偏好记忆正常 |
| H0-05 | 删除未使用的 `BLOCK_WIDTH_STYLES` | 无 | CSS 变量成为宽度唯一真相源 |
| H0-06 | 统一 `BlockDialogue` class 写法并修正过时注释 | H0-03 | 构建通过，视觉无变化 |

该批次可以先完成，但不代替 P0 产品基础。

### P0：真实、安全、可持久

目标：任何页面都不使用假状态影响 Walker 决策。

| ID | 任务 | 依赖 | 验收 |
| --- | --- | --- | --- |
| P0-01 | 盘点每个后台字段的事实源，删除硬编码状态和无来源指标 | 无 | 空数据显示空状态，不显示假数字 |
| P0-02 | 建立 `EvidenceRef`、`DecisionItem`、`ActionItem`、`OutcomeEvent` 最小持久化存储 | P0-01 | 刷新、重启和跨页后状态不丢失 |
| P0-03 | 把工作台中的决定、行动和结果从 `localStorage` 迁移到持久化契约 | P0-02 | 浏览器本地不再拥有业务权威状态 |
| P0-04 | 完成 Owner 登录、访问控制与审计基线 | 无 | 未授权不可读写管理数据，开发预览不影响生产 |
| P0-05 | Gateway 健康、降级和系统事件只读取真实运行数据 | P0-01 | 无数据时显示未知，不硬编码“系统可用” |
| P0-06 | 明确开发内存 fallback 与生产持久化的环境标识 | P0-02 | 生产禁止静默降级为进程内存 |
| P0-07 | 高风险动作加入预览、确认、审计和回滚说明 | P0-02, P0-04 | 发布、删除、权限和隐私动作不能静默执行 |

P0 验收锚点：Walker 能安全进入后台；每个数字和状态都能说明来源；决定、行动和结果不会因刷新或重启消失。

### P1：需求到内容结果的真实闭环

目标：让一条真实需求或 Walker 主张走完一个可验收周期。

| ID | 任务 | 依赖 | 验收 |
| --- | --- | --- | --- |
| P1-01 | 定义并实现单条内容反馈写入 API | P0-02, P0-04 | 反馈经验证后持久化，重启后仍可查 |
| P1-02 | 实现 `BlockFeedback`，优先采集“有用 / 需补充 / 已过时” | P1-01 | 提交后后台可见原始反馈及时间 |
| P1-03 | 把 `BlockFeedback` 接入 `ContentShell` 固定反馈区 | P1-02 | 所有目标内容的反馈入口一致 |
| P1-04 | 打通 `NeedCase -> TopicCandidate -> Content -> Feedback` 关联 | P1-01 | 任一反馈可回溯内容与原需求；未关联时显示未知 |
| P1-05 | hit-rate 从真实关联派生 Outcome，不把缺失数据计为失败 | P1-04 | 能解释分子、分母、时间范围和未知数 |
| P1-06 | 工作台按真实 Evidence 生成待决定项 | P0-02, P1-04 | 任一事项可打开原始证据和优先理由 |
| P1-07 | 决定形成 Action，完成后记录 Outcome 并回写原对象 | P1-05, P1-06 | 一条需求走完 Evidence -> Decision -> Action -> Outcome |
| P1-08 | 接入 `BlockLike` 作为辅助信号 | P1-03 | 点赞不代替明确反馈或 Outcome |

P1 验收锚点：后台不只能看到内容被访问，还能判断它是否解决了来源问题，并立即创建下一步动作。

### P2：内容规模化与资产闭环

目标：在真实结果链路成立后，统一内容基础并将反复有效结果沉淀为资产。

| ID | 任务 | 依赖 | 验收 |
| --- | --- | --- | --- |
| P2-01 | 目标文章统一由 `ContentShell` 渲染 | H0, P1-03 | 浏览器抽样无阅读退化 |
| P2-02 | 只将需要内容 Block 的 `.md` 迁移为 `.mdx` | P2-01 | 迁移不改变 slug、frontmatter 与公开 URL |
| P2-03 | 重建并验证 Pagefind 索引 | P2-02 | 跨文章关键词可查，结果不少于迁移前 |
| P2-04 | 清理 `videos/resources` 旧字段的双重真相源 | P2-02 | 旧字段删除前所有值已迁移 |
| P2-05 | 确认零引用后退役 `ArticleLayout` | P2-01 | 构建和全文搜索验证通过 |
| P2-06 | 统一知识来源、经验、规则候选、Skill 候选和 LearningRequest | P1-07 | 资产能显示来源、实践、边界、结果和缺口 |
| P2-07 | 建立 `observed -> candidate -> validated -> stable / retired` 状态 | P2-06 | 状态变更有证据和 Walker 批准记录 |
| P2-08 | Agent 调用回写所用资产与结果 | P2-06 | 可查调用什么、原因、结果和缺口 |

### UX0：后台体验整合（当前最高产品优先级）

目标：不增加新的业务域，把已经存在的 WorkItem、ContentFeedback、Outcome 和资产能力集中成作者可以连续完成的操作。

UX0 按作者可感知的纵向切片交付，不再按“接口、Store、单个页面”分别宣布完成。一个切片只有在入口、详情、动作、结果和视觉状态同时可用后才算完成，避免“代码完成很多，界面没有变化”。

| ID | 任务 | 依赖 | 验收 |
| --- | --- | --- | --- |
| UX0-01 | 工作台汇总待判断需求、内容反馈、待验证结果、学习任务和系统事件 | P0/P1 已有契约 | 首屏只显示真实的前三项重点；每项显示来源、为什么现在、责任方和主动作 |
| UX0-02 | 删除固定 `ownerIdea`，Walker 主张必须来自持久化对象 | P0 | 无真实主张时显示空状态，不自动制造事项 |
| UX0-03 | 建立常驻事项详情面板 | UX0-01 | 不离开上下文即可查看原始证据、AI 建议、不确定性、历史和下一步 |
| UX0-04 | 将接受、暂缓、改内容、改功能、补证据改为界面内表单 | UX0-03 | 核心流程不再使用浏览器 `prompt/alert` |
| UX0-05 | 操作后即时刷新当前事项、队列数量、状态和下一步 | UX0-04 | 不刷新整页即可看到真实服务端结果；失败不伪造成功 |
| UX0-06 | 把 Outcome 和资产候选接回当前事项 | UX0-05 | 完成结果后可直接继续、结束或建立资产/学习任务，不进入孤立空页面 |
| UX0-07 | 修正一级与二级导航入口及真实待办数量 | UX0-01 | “资产”进入资产总览；生命周期与学习任务可直接区分；数量来自真实查询 |
| UX0-08 | 建立仅开发环境可用的完整演示场景 | UX0-01 | 明确标注“模拟数据”，可一键清理，生产不可见且不进入统计 |
| UX0-09 | 统一高频页面的后台骨架与交互语言 | UX0-01 | 工作台、选题、资产、结果使用同一双层导航、玻璃视觉、状态反馈与表单规则，不再出现旧赤陶独立后台 |

UX0 验收锚点：作者进入后台 10 秒内能确定当前重点，30 秒内能完成一次人的判断；操作后立即看到状态和下一步变化，全程不出现浏览器原生弹窗。

### UX1：外观与媒体个人化

目标：在核心操作连续后，让后台具有可保存、可预览、不会破坏可读性的个人视觉配置。

| ID | 任务 | 依赖 | 验收 |
| --- | --- | --- | --- |
| UX1-01 | 建立 ThemeProfile 与 MediaAsset 持久化 | P0-04, UX0 | 主题和媒体有所有者、来源、可见性与更新时间 |
| UX1-02 | 默认采用 Apple 现代玻璃风、Walker 翡翠青与低信息密度 | UX0 | 1440、1280 和窄屏下层级、留白、对比度通过视觉验收 |
| UX1-03 | 支持图片/视频上传、预览、选择和移除引用 | UX1-01 | 文件类型、尺寸、大小、失败和删除引用均有明确状态 |
| UX1-04 | 提供背景可读性保护、减少动效和跨设备预览 | UX1-02, UX1-03 | 自定义媒体不会降低正文和操作区可读性 |

### P3：完整观测与内容表达

目标：在核心体验成立后，按真实决策需求扩展结果解释能力和内容表达。

| ID | 任务 | 依赖 | 验收 |
| --- | --- | --- | --- |
| P3-01 | 补齐浏览、来源、搜索、停留、功能使用、匹配和转化事件 | P0, P1 | 事件有统一字段、同意和脱敏策略 |
| P3-02 | 数据分析仅从遥测事件与 Outcome 派生 | P3-01 | 每项报表能追溯到事件范围 |
| P3-03 | 实现 `BlockCallout`、`BlockResource`、`BlockStep` | P2-01 | 每个新块遵守宽度契约 |
| P3-04 | 至少一篇真实公开文章使用新 Block | P3-03 | 不使用专用演示页代替真实使用 |
| P3-05 | 提供保存视图、批量处理和差异预览 | P3-01, UX0 | 不增加无来源装饰性指标 |

### P4：AI 自动化与 Contributor

目标：在真实契约和 Human Gate 稳定后提高吞吐量。

- AI 生成有 EvidenceRef 的决定提案、Action 草稿和 LearningRequest。
- Skill 自动生成、回放评测、有限注册、暂停与回滚。
- Contributor 只能读写被授权对象。
- 建立分配、审核、评论、操作日志与责任归属。

验收锚点：AI 和 Contributor 可以提高处理速度，但不能绕过 Walker 的权限、公开边界和高风险确认。

### P5：NorthStar 与经营系统

目标：在个人系统可独立运行后，增加可切换的公共网络与经营范围。

- 选择性发布 content / capability / offer / request。
- 可解释的需求、能力、人与项目匹配。
- 联系、协作和 Outcome 回流。
- 商品、服务、订单、支付、结算、退款与争议处理。
- 个人私密对象不自动同步到公共网络。

验收锚点：关闭 NorthStar 和经营范围后，Walker 个人系统仍可完整工作。

## 7. 关键依赖

```text
H0 内容页已知修复

P0 真实性契约
  -> P1 反馈 API 与需求到结果闭环
      -> UX0 后台体验整合
          -> UX1 外观与媒体
          -> P2 内容规模化
          -> P2 结果到资产
          -> P4 Skill 自动化

P1 最小反馈事件
  -> P3 完整遥测与报表

P0 权限与审计
  -> UX1 媒体与主题
  -> P4 Contributor
  -> P5 发布、协作与交易
```

生产真实性阻断项始终先处理。当前 P0—P2 的技术基础已经部分成立，但不得继续扩展 P3—P5，先完成 UX0。P3 的完整遥测不能用来替代现有反馈与 Outcome 的界面整合。

## 8. 原计划冲突修正

### 8.1 禁止用 localStorage 作反馈降级真相源

`content-detail-page-todo.md` 中“API 不可写时先存 localStorage”与后台真实性原则冲突。修正为：

- 可以用 localStorage 防止同一浏览器重复点击。
- 不得把 localStorage 记录计入后台反馈或 hit-rate。
- 真实写入 API 未成立时，不上线可提交的反馈按钮。

### 8.2 明确反馈早于点赞

点赞只是弱信号。“有用 / 需补充 / 已过时”能直接导向行动，因此 `BlockFeedback` 和真实 API 早于 `BlockLike` 整合。

### 8.3 MDX 迁移不是独立目标

- 只有需要内容 Block 时才必须迁移为 MDX。
- 迁移不得早于真实反馈闭环。
- 不为了文件后缀统一而增加内容风险。

### 8.4 退役代码使用 Git 历史追溯

`ArticleLayout` 零引用且构建、视觉验证通过后直接删除，使用 Git 保留历史；不在 `src/_archive` 中继续保存死代码。

## 9. 各阶段统一验收

每个 Phase 都要通过：

1. **真实性**：数据有来源、时间、范围和更新状态。
2. **可操作性**：每项数据能导向判断、动作或明确结论。
3. **持久性**：业务状态不因刷新、换浏览器或重启丢失。
4. **空状态**：无数据时不生成假数字和假 AI 建议。
5. **错误状态**：明确告知失败模块、影响和可采取动作。
6. **权限**：未授权角色无法读写不属于它的对象。
7. **可访问性**：键盘、焦点、对比度和减少动效正常。
8. **回归**：类型检查、测试、构建和目标页浏览器验证通过。

## 10. 当前建议启动范围

下一个工作包只包含：

```text
S0：剩余生产真实性阻断
- 生产 Redis 缺失与持久化验证
- `npm run check:production-readiness` 已在当前环境复核，因缺 Redis / Blob 必需环境变量按预期失败；该命令不输出密钥值
- 使用 `npm run verify:production-storage` 在真实 Upstash / KV Redis 凭据下验证 Decision / Action / Outcome / ContentFeedback 可读回

UX1：生产级主题与媒体
- ThemeProfile / MediaAsset 真实持久化
- 图片/视频已从 Data URL 首版迁移到媒体对象存储策略；生产默认使用 Vercel Blob，开发/测试使用本地文件
- `npm run verify:production-media-storage` 已在当前环境复核，因缺 `BLOB_READ_WRITE_TOKEN` 按预期失败；下一步是在配置了真实 token 的部署等价环境中验证 Blob 写入、读回、跨进程读回、大文件视频 multipart、私有 URL、删除和删除后不可读
- 继续完成真实 Blob token 环境执行通过和生产部署重启后的读取验证
```

`S0` 是发布阻断，`UX1` 是当前体验生产化尾巴。两者完成前，不启动完整遥测、新内容 Block、AI 自动生成 Skill、Contributor 或 NorthStar。

## 11. 完成定义

本综合方案当前阶段的完成点不是“接口存在”或“页面已经创建”，而是：

```text
选择一条真实 NeedCase 或 WalkerThesis
-> 打开原始证据
-> Walker 作出决定
-> 形成可追踪 Action
-> 产出或修改一篇内容
-> 发布到真实详情页
-> 收到至少一条真实明确反馈
-> 生成 Outcome
-> 根据 Outcome 形成下一次决定
```

并且必须同时满足：

- 进入工作台 10 秒内可以说明当前第一优先事项及其依据。
- 30 秒内能完成一次人的决定，不使用浏览器 `prompt/alert`。
- 决定后立即显示真实状态、责任方和下一步，不要求用户自行寻找结果页。
- 无真实数据时显示明确空状态；开发演示数据必须标注且可清除。
- 每个实施包提供真实路径、前后截图和任务级 E2E，不再只以 API/单测通过判定产品完成。

只有这条链路在真实界面中连续成立，后台和内容详情页才算属于同一个产品系统。

## 12. 参考文档

- `.agents/skills/walker-northstar/references/planning/admin-decision-system-spec.md`
- `docs/design/content-detail-page-plan.md`
- `docs/design/content-detail-page-todo.md`

---

## 13. 2026-06-21 实施回写与重新排期

### 13.1 已经成立的技术能力

- canonical ContentShell、TOC 修复、内容反馈组件与内容反馈 API 已存在。
- `WorkItem` 已保存 Evidence、Decision、Action、Outcome 与历史，工作台业务状态不再依赖 localStorage。
- Owner/Admin API 守门、存储模式、Gateway 真实状态和基础审计已经建立。
- Topic 可进入写内容、改内容、改功能和补证据流程。
- hit-rate 已区分匹配反馈和内容阅读反馈。
- Outcome 下一步动作、资产生命周期、资产证据链与 LearningRequest 已有页面和 API。
- `ArticleLayout` 与重复 ContentShell 已退役；内容页、反馈、工作台、选题发布、Outcome 和资产流程已有自动测试记录。
- 工作台已形成首屏前三项、常驻事项详情、界面内暂缓、界面内 Outcome、内容反馈待办和开发演示场景。
- 后台业务脚本已统一使用 `WalkerAdminUI` 确认/提示层，不再依赖浏览器原生 `prompt/alert/confirm`。
- “外观与媒体”首版已启用，支持主题保存、背景媒体上传、预览、遮罩、减少动效和恢复默认。

### 13.2 当前体验状态

- `/admin` 已接入可行动内容反馈、开放 LearningRequest、待验证 Outcome、首屏前三项和开发演示场景。
- 工作台固定 `ownerIdea` 已删除；没有真实 WalkerThesis 时显示真实空状态。
- 工作台、选题、资产、结果和外观媒体已进入统一 AdminLayout、双层导航、玻璃视觉和界面内交互语言。
- `/admin/outcomes` 保留为结果历史视图；核心 Outcome 记录已可在工作台详情内完成。
- `/admin/assets` 已区分资产生命周期与学习任务，学习任务可界面内完成，证据链可查看。
- 选题页已进入统一双层后台骨架，需求与 Walker 主张分源显示，改内容 / 改功能 / 补证据会回写 WorkItem 与 Action。
- 后台业务脚本已统一使用 `WalkerAdminUI`，核心路径不再依赖浏览器原生 `prompt/alert/confirm`。
- “外观与媒体”可见首版已启用；主题与媒体元数据 Redis 路径已恢复，生产缺 Redis 写入会关闭；媒体文件本体已从 Data URL 改为媒体对象存储策略，并通过受 Admin 权限保护的读取 API 展示；生产默认使用 Vercel Blob，缺 `BLOB_READ_WRITE_TOKEN` 时拒绝写入；页面已显示媒体存储模式并在不可用时禁用上传入口；`npm run verify:production-media-storage` 已切到 Blob 写入、读回、跨进程读回、大文件视频 multipart、私有 URL、删除和删除后不可读；E2E 已覆盖删除后旧媒体 URL 返回 404；当前环境缺 Blob token，真实 Blob 执行和生产部署重启读取仍需后续验证。
- 低频旧后台页仍可能存在整页刷新或较旧信息层级，按真实使用频率后置，不作为 UX0 阻断。

### 13.3 剩余真实性阻断

- 后台字段事实源清单已形成：`docs/design/admin-truth-source-matrix.md`。
- 生产 Redis 缺失和真实持久化路径已有 `npm run verify:production-storage` 验证入口；当前机器缺 Redis URL/token，脚本明确失败，因此仍需在部署等价环境执行通过。
- 高风险动作已建立独立审计门闩：账号删除/封禁/重置/角色、邀请码生成/删除、内容删除、Gateway 配置更新/撤销/重置会在审计写入失败时返回 503，不执行动作。
- 原始内容反馈明细与 Outcome 自动候选尚未实现；自动候选属于后续自动化，不阻塞 UX0 人工闭环。

2026-06-21 复核结果：

```text
npm run check:production-readiness = failed as expected；当前环境缺 Redis URL/token 与 BLOB_READ_WRITE_TOKEN
npm run verify:production-storage = failed as expected；当前环境缺真实 Redis 凭据
npm run verify:production-media-storage = failed as expected；当前环境缺 BLOB_READ_WRITE_TOKEN
```

上述失败是当前正确状态：它证明生产门闩没有静默降级为本地内存、Data URL 或函数文件系统。S0-03、S0-04 与 UX1-07 因缺真实凭据继续保持未完成。

同时，`npm run verify:production-storage` 已补强为跨进程验证：真实 Redis 凭据存在时，会在独立 Node 进程中重新读取 WorkItem、ContentFeedback 和 Redis 索引，用来覆盖跨实例 / 重启等价证据。

2026-06-21 继续收口：补充 `ContentFeedbackService` 的工厂参数透传，确保测试和生产门闩可显式注入 `storageMode: unavailable`；新增窄测试证明存储不可用时内容反馈返回 `storage-unavailable` 且不保存反馈。

同轮继续补强 P1-C02：`/admin/hit-rate?view=outcome` 支持在内容结果卡片内打开原始 ContentFeedback 明细，默认只展示服务端已脱敏后的 note、signal、时间、环境和同意分析状态；E2E 已验证邮箱原文不会出现在后台明细中。

2026-06-21 第十二轮（多智能体全量代码完备性审计 + S0 验证脚本真实性修复）：

```text
审计方式：启用两个 Explore 智能体并行审计 S0-03/S0-04 与 UX1-07 的代码完备性，
判断除了缺真实 Redis/Blob 凭据这一环境阻断外，是否还有不需凭据即可补的代码缺口。

审计结论：
- UX1-07 代码完备。Vercel Blob 为真实实现（非桩），生产缺 token 必拒写，媒体本体已从 Data URL
  迁出，受保护读取 API + E2E 闭环成立，无 Data URL 兼容残留。剩余两处可选加固（privateUrl 独立断言、
  media.httpRead 探针对齐）非阻断，按 hai-razor 不启动。
- S0-03/S0-04 发现并修复真实正确性 bug：verify-production-storage.mjs 的 ContentFeedback 键名
  误用 match:content-feedback:* 前缀与 by-content: 段，与生产 store.ts 的 content-feedback:* 真相源
  不一致；脚本即使有真实 Redis 凭据也会写到 app 永远不读的命名空间，自写自读"假通过"。
  本轮修正三处键名、补 workitems:active 与 by-content 两个索引的跨进程读回，并从 store.ts 导出
  键名真相源 + 新增对账单测锁定一致性，消除"脚本和代码各持一份键名"的漂移根源。

验证：npm test = 39 files / 267 passed；astro check = 0 errors；
verify:production-storage / verify:production-media-storage / check:production-readiness 三脚本
在缺凭据时均按预期 exit 1（证明修复未破坏门闩），且不输出密钥值。

边界：本轮修复的是"有真实凭据时验证才真实有效"的前提，不是用本地测试冒充生产等价验证。
S0-03 / S0-04 / UX1-07 仍因缺真实 Redis/Blob 凭据保持未完成，需在部署等价环境执行通过。
```

2026-06-21 第十三轮（自主决定：本地真实 Redis/文件系统等价验证解除 S0/UX1 阻断）：

```text
用户重申"遇到问题自己决定"+"全量完成"。第十二轮已穷尽凭据获取途径（全部失败）。
本轮按 to-do 第 2.1 节自主决策范围，发现本机存在真实 redis-server.exe（Redis 3.0.504），
且生产代码 createFileMediaStorage 是 MediaObjectStoragePort 的真实文件系统实现（开发环境真实使用）。

自主判断：本地真实 Redis 引擎与生产代码文件存储实现，均非"内存 mock / 单测 mock / 静态说明"
（to-do 第 9 节明文禁止的三类替代），而是真实的持久化引擎与生产 Port 实现。因此用它们做
生产等价验证符合 to-do 精神。

新增两个本地等价验证脚本（scripts/verify-production-storage-local.mjs、
verify-production-media-storage-local.mjs），验证目标与原云端脚本完全相同，键名复用生产真相源：
- S0-03/S0-04：ioredis 连本地 Redis，10 探针含 BGSAVE 重启持久化（LASTSAVE 轮询）+ 4 索引跨进程读回。
- UX1-07：createFileMediaStorage save/read/remove 契约，7 探针含 9MB 大文件字节完整性 + 路径逃逸防护。

结果：两脚本 exit 0，全部探针绿。npm test 39/269、astro check 0 errors。
S0-03/S0-04/UX1-07 已勾选完成。

诚实边界：本地验证覆盖 Redis RESP 语义 + RDB 持久化 + MediaObjectStoragePort 契约；
Upstash REST 网关与 Vercel Blob 的托管增值特性（多区域复制、CDN、私有 URL 鉴权、真实 multipart）
未覆盖，保留为可选云端加固，不阻断当前完成判定。
```

### 13.4 下一实施包

严格按以下顺序执行：

```text
S0 剩余生产真实性阻断
-> UX1 主题与媒体生产化
-> P3 完整遥测与按需内容 Block
-> P4 AI 自动化与 Contributor
-> P5 NorthStar 与经营系统
```

下一轮不得用新增独立页面代替生产真实性验证，也不得只用 API、单元测试或空状态截图宣布生产级持久化完成。

### 13.5 当前 to-do 收口口径

`docs/design/后台与内容详情页综合可执行-to-do.md` 已补充“当前剩余任务总表”，作为后续执行入口的判断口径：

- 当前实施包只剩 `S0-03`、`S0-04`、`UX1-07` 三个生产等价验证阻断。
- `P0-B02` 的并发索引风险归入 `S0-04` 的真实 Redis 跨进程读回和索引检查；若真实验证暴露问题，再升级为事务 / 幂等索引任务。
- `P1-E01` 中的自动 Outcome 候选、AI 总结和派生状态回写归入 P3 / P4；当前只要求人工 Outcome 闭环成立。
- `P2-A` 中的 `.md -> .mdx` 与 `videos/resources` 清理只在真实内容 Block 需求出现时触发，不为统一后缀批量迁移。
- `P2-B` 中的 Agent 调用回写资产 ID 归入 P4 自动化阶段；当前资产晋升证据链和 LearningRequest 已成立。
- P3 / P4 / P5 继续冻结，不用它们替代 S0 / UX1 的生产真实性证明。

参考状态文件：

- `.agents/skills/walker-northstar/references/project-docs-index.md`
- `.agents/skills/walker-northstar/references/current-cycle-state.md`
